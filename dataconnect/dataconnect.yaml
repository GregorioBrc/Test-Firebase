specVersion: "v1beta"
serviceId: "test-firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "test-firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
